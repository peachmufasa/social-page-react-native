# React Native social network page

Features added:
+ Gradients transition from avatar to app body background;
+ Reusable interactive button component with customizable icons and messages caused on button press;
